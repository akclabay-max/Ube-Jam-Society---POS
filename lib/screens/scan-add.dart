import 'package:flutter/material.dart';

class ScanAddScreen extends StatelessWidget {
  const ScanAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Scan Add')),
    );
  }
}